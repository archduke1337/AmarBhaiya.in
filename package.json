{
  "name": "@amarbhaiya/workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "analyze": "turbo analyze",
    "setup:appwrite": "node tooling/scripts/setup-appwrite.mjs"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "turbo": "^2.10.9"
  }
}
